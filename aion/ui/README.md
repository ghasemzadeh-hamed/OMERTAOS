# AION Catalog UI

This directory is reserved for the Next.js implementation of the catalog surfaces
(Grid, ToolView, Editor, Chat Assistant). The frontend is not included in this
commit, but the backend APIs, configuration, and worker synchronization layers
are ready to support it.
