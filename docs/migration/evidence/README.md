# Structure migration evidence

This directory preserves small, inactive artifacts that were removed from the
repository root during Structure S6. Files here are historical evidence only;
they are not runtime configuration, importable application code, or supported
deployment entrypoints.

The verified external backup and Git history remain the authoritative recovery
sources. Keeping these artifacts under `docs/migration/` prevents them from
being mistaken for canonical owners while preserving review context.

