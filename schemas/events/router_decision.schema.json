{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RouterDecisionEvent",
  "type": "object",
  "required": ["task_id", "intent", "route", "reason", "tier", "timestamp"],
  "properties": {
    "task_id": { "type": "string" },
    "tenant_id": { "type": "string" },
    "intent": { "type": "string" },
    "route": { "type": "string" },
    "reason": { "type": "string" },
    "tier": { "type": "string" },
    "timestamp": { "type": "string", "format": "date-time" }
  }
}
